@{
    ViewBag.Title = "User List";
}
@model IList<UserViewModel>

<div class="page-header">
    <h1>User List</h1>
</div>

<form asp-action="list">

    <div class="table-toolbar clearfix row">
        <div class="col-md-9">
            <div class="btn-group btn-group-sm">
                <a class="btn btn-info" asp-action="create"><i class="glyphicon glyphicon-plus"></i> New</a>
                <button type="submit" class="btn btn-danger" asp-action="Deletes">Delete</button>
            </div>
        </div>
        <div class="col-md-3">
        </div>
    </div>

    <table class="table table-bordered table-striped table-item-list">
        <thead>
            <tr>
                <th class="item-checkbox"><input type="checkbox" /></th>
                <th class="item-title">UserName </th>
                <th class="item-title">Email </th>
                <th class="item-title">PhoneNumber </th>
                <th class="item-title">DisplayName </th>
                <th class="item-title">Lockouted </th>
            </tr>
        </thead>

        @if (Model.Count == 0)
        {
            <text>
            <tr>
                <td colspan="10" class="text-center">Empty</td>
            </tr>
            </text>
        }
        else
        {
            foreach (var item in Model)
            {
                <text>
                <tr>
                    <td class="item-checkbox"><input type="checkbox" name="itemId" value="@item.Id" /></td>
                    <td class="item-title">
                        <a asp-action="edit" asp-route-id="@item.Id">@item.UserName </a>
                    </td>

                    <td>@item.Email</td>
                    <td>@item.PhoneNumber</td>
                    <td>@item.DisplayName</td>
                    <td>@item.Lockouted</td>
                </tr>
                </text>
            }
        }

    </table>

</form>
